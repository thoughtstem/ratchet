#lang info

(define scribblings 
  '(("scribblings/manual.scrbl" ())))

(define deps '("read-lang-file"))

(define drracket-tool-names (list "Tool Name"))
(define drracket-tools (list (list "tools/main.rkt")))
